# 结构

与卡片 UI 的“结构”分组一致。这些卡片负责随机排布、层状复制和逐原子结构分组：
`Random Packing` 在给定晶胞中生成随机排布，`Layer Copy` 复制完整层状结构，
`Layer Groups` 沿晶面法向识别原子层并写入逐原子 A/B 标签。

```{toctree}
:maxdepth: 1

../cards/random-packing-card
../cards/layer-copy-card
../cards/group-label-card
```
