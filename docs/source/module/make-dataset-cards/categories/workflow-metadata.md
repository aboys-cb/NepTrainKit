# 工作流与元数据

这组卡片不表达某一种材料结构操作，而是控制后续卡片如何识别原子集合、如何分支以及如何合并输出。

| 目标 | 使用卡片 |
| --- | --- |
| 给原子写入可复用的 `group` 标签 | `Group Label` |
| 让同一输入进入多个分支并汇总结果 | `Card Group` |

```{toctree}
:maxdepth: 1

../cards/group-label-card
../cards/card-group
```
