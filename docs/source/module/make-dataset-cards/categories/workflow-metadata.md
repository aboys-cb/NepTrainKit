# 容器

与卡片 UI 的“容器”分组一致。这些卡片不改变材料结构，而是控制同一输入如何分支、
如何保持独立流程，以及何时合并输出。

| 目标 | 使用卡片 |
| --- | --- |
| 让同一输入进入多个分支并汇总结果 | `Card Group` |
| 让多个分支继续独立处理并分别输出或显式合并 | `Permanent Fork` |

```{toctree}
:maxdepth: 1

../cards/card-group
../cards/workflow-fork
```
