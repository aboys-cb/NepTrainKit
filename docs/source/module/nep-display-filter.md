# 筛选栏：按标签、元素和表达式找结构

这一页对应结构筛选栏。简单条件优先使用配置类型、化学式和元素筛选；需要组合数值条件时，
再使用 `expression`。筛选只改变匹配或选择状态，不会自动删除结构。

```{contents} 本页目录
:local:
:depth: 3
```

```{include} nep-dataset-display-content.md
:start-after: <!-- display-filter-start -->
:end-before: <!-- display-filter-end -->
```
